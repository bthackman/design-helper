# Close-out — [Project name]

Written once, when `state.json`'s project-level `status` moves to `complete` or `dormant` — not a
running log, a single look-back. Keep it a table, not a system: the point is capturing calibration
data cheaply while it's fresh, not building a second decision log. If the project is dormant rather
than finished, fill what's known so far and revisit at close.

## Phase timing
| Phase | Expected (if estimated up front) | Actual | Notes |
|---|---|---|---|
| Client discovery |  |  |  |
| Site |  |  |  |
| Precedents |  |  |  |
| Massing |  |  |  |
| Space planning |  |  |  |
| Materiality |  |  |  |

## Driver-proxy vs. reality
Only fill rows where a phase made a falsifiable proxy claim that later phases (or, ideally, the
built project) could actually test — the Massing Studio's driver proxies are the clearest source of
these; not every driver produces one.

| Driver | Proxy claim (phase, what was claimed) | What actually held once tested further / built | Delta / lesson |
|---|---|---|---|
|  |  |  |  |

## Promoted items that got reused
Of what this project promoted to `Library/` or the vault, what (if anything) a *later* project
actually pulled back in — this is the only place that loop closes. Leave blank if it's too soon to
know (the promoting project has to come before the reusing one).

| Promoted item | Promoted to | Reused by (later project) | How |
|---|---|---|---|
|  |  |  |  |

## One-line takeaway
If future-you reads only one sentence from this file, what should it say —
