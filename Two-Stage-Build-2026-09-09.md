# Two-stage massing — the build (2026-09-09)

**What this is.** The implementation pass. `Two-Stage-Integration-2026-09-09.md` §7 Stages 2–5 built, verified through a real headless Chromium (Playwright 1.62.0, `C:\Users\bthac\AppData\Local\Programs\Python\Python310\python.exe`), not read from the code. Stages 0–1 were already done and are untouched by this pass (re-confirmed: `Test-Fit-Studio.html:885-896` still carries the "NO CONTAINMENT CLAMP" comment; `addRoom()`/`deleteRoom()` still at `Test-Fit-Studio.html:1028-1056`).

**Files touched** (everything else in the repo, including `HANDOFF.md`, is untouched):
- `Projects/Nonimuss-Residence/3_Massing/Massing-Studio.html` — Stage 2/3/4 mechanism
- `Projects/Nonimuss-Residence/4_Space-Planning/Test-Fit-Studio.html` — one guarded cross-call
- `Projects/Nonimuss-Residence/Design-Studio.html` — **regenerated**, never hand-edited, via `node _UI/build-design-studio.js` (run after every source edit, confirmed idempotent by a final re-run before writing this report)
- `_skill-source/references/phase-3-massing.md`, `_skill-source/references/phase-4-space-planning.md`, `_skill-source/SKILL.md`, `GLOSSARY.md` — Stage 5 method propagation
- `design-process.skill` — repackaged via `python -m zipfile` (never `Compress-Archive`)

---

## Stage 2 — Promotion preview (no write)

**Built:** `testFitBBoxFor(type)` (`Massing-Studio.html:780-793`) — the mirror image of `Test-Fit-Studio.html`'s own `liveCutForLevel()`: it feature-detects Test-Fit's `rooms`/`ROOM_VOLUME_TYPE` globals from the Massing side (the first read in this codebase running *into* Test-Fit's object graph rather than out of it), and is a safe no-op wherever those globals don't exist (standalone `Massing-Studio.html`). `promotionPreview()` (`:799-807`) aggregates, per volume, the bounding box of every matched Test-Fit room and the delta against the volume's current `w/d/x/z`. `refreshPromotionPreview()` (`:809-826`) renders that into a new `#promoPreview` panel (HTML at `:88`) naming both the box delta **and** which metrics go stale on commit for COND-type volumes (`facade, effLoss, glazedPct, D3` — `STALE_ON_PROMOTE` at `:242`), plus a distinct amber 3D ghost (`rebuildPromoGhosts()`, `:830-847`, its own `promoGhostGroup` — separate from Stage 0's tan file-sync ghost, which is untouched). Wired into both directions live: Massing's `refresh()` calls it (`:405`), and `Test-Fit-Studio.html`'s `render()` calls it back, guarded (`Test-Fit-Studio.html:760-767`), so dragging a room in the Test-Fit pane updates Massing's preview in the same instant, and vice versa.

**Checkpoint, run in `Design-Studio.html`:**
- **Preview fires with no interaction needed at all** — House's real conflict #1 (documented since Step 1: rooms need 13.0×12.3 m, box is 11.6×9.3 m) is already live on page load. Panel text at boot: *"House: 11.6×9.3 → 13.0×12.3 m … Committing goes stale: facade, effLoss, glazedPct, D3 and the trade-space bubble/D3 bar (Stage 2 / LOD 200 — [schematic-only — see Stage 2])."*
- **Liveness, driven with a real mouse drag, not a function call:** dragged room ML (Mech/laundry, the room whose south edge sets the House bbox's `maxY`) 1.68 m further south with real `page.mouse.down/move/up` events. Preview text updated live to *"House: 11.6×9.3 → 13.0×13.6 m"* — a genuinely different number, confirming the mechanism reads live geometry, not a cached value. Restored ML to its exact original position; preview text returned to byte-identical with the pre-drag text.
- **`state` (Massing's) provably unchanged** throughout: `JSON.stringify(state)` captured at boot, after reading the preview panel, and after the ML drag+restore — identical every time, across two independent passes.
- **Screenshots:** `_UI/_playwright/qa-scratch/stage2_massing_only_preboot_ghost.png` (Massing-only view — badge, preview panel, amber ghost box, and untainted metrics all visible together, pre-promotion) and `stage2_preview_live_after_drag_pass1.png` (Plan-only view mid-drag, showing the live massing-cut overlay and the four pre-existing containment breaches: MB/GB/MU/ML).
- **A real finding while building this test, worth Ben knowing:** dragging ML at its natural screen position initially hit the Checks panel (`#right`), not the room — `elementFromPoint` returned a `<td>` from the adjacency table. ML sits far enough east/south that even "Plan only" layout mode doesn't clear the panel over it; had to collapse both side panels to reach it. This is the same class of crowding `Review-Steps-1-4-2026-09-09.md` already measured (the ~41px-clear-width finding), now confirmed to also bite at the *opposite* (east/south) edge, not just centrally.

---

## Stage 3 — The commit ("Reshape to fit")

**Built:** `promoteVolume(i)` (`Massing-Studio.html:851-870`) writes new `w/d/x/z` **and** `state[i].promoted = {from:'test-fit', date, before}` as one atomic assignment, then calls the exact `rebuild(); refresh();` pair any slider's `oninput` already calls — no special-case render path. `metricsDisplay(m, state)` (`:233-262`) is the new wrapper: `metrics()`/`drivers()` are **byte-identical** to before this pass (confirmed by direct text comparison against the first full read of this file at the start of this session — every line of both function bodies matches exactly; I only added code around them, in a new function, never inside them. Note: this repo's git history is well behind its working tree from several prior uncommitted sessions, so a raw `git diff` against HEAD is not a clean before/after for *this* pass alone — direct comparison against my own session-start read is the reliable check here). The wrapper gates `facade`/`effLoss`/`glazedPct` to the tagged sentinel `[schematic-only — see Stage 2]` and captions `coverage`/`nVol` (via `title` tooltips) the moment any COND-type volume (`house/office/pool/link`) carries `.promoted`. Wired into `refresh()` (`:384-405`) for the metrics panel, and into `updateTrade()` (`:520-554`) for the trade-space bubble/D3 bar and readout line.

**Checkpoint — House promoted, per `Two-Stage-Massing`'s own conflict #1:**

| | Before | After (promoted) | Hand-calc | App output |
|---|---|---|---|---|
| House `w×d` | 11.6×9.3 | **13.0×12.3** at x18,z5 (unchanged position) | 13.0×12.3 | 13.0×12.3 ✓ |
| `dwell` | 118.98 | 171.0 | 159.9+11.1=171.0 | 171 ✓ |
| `volume` | 1037.82 | 1245.9 | 639.6+378+33.3+120+75=1245.9 | 1245.9 ✓ |
| `coverage` | 19.30% | 23.61% | 284.9/1206.70×100=23.61% | 23.6098% ✓ |
| `facade` | 507.4 | 542.6 | 202.4+40.2+180+120=542.6 | 542.6 ✓ |
| `effLoss` | 1048.0 | 1109.6 | 354.2+80.4+405+270=1109.6 | 1109.6 ✓ |
| `glazedPct` | 42.62% | 41.80% | 226.8/542.6×100=41.80% | 41.7987% ✓ |
| `nVol` | 5 | 5 (unchanged — box-grow, not box-per-room) | 5 | 5 ✓ |

Every one of these was computed independently by hand (real perimeter/area arithmetic from the DEFAULTS.B box list — see the "spot-check" work log in this session, not reproduced here) **before** reading the app's own output, and every one matches to the last decimal the app's floating-point rounding allows. `LOT_AREA` used in the coverage hand-calc: shoelace formula on `LOT=[[0,0],[45.83,0],[45.83,19.83],[0,32.83]]` = 1206.70 m², independently re-derived, matching the app's internal value implied by its own coverage% output.

- **`metrics()`'s raw shape unchanged**, confirmed by reading its direct output before and after promotion — it still returns the same eight fields, still computes `facade`/`effLoss` for real (542.6/1109.6 above are `metrics()`'s own raw numbers, read directly — the wrapper doesn't touch them, it only decides what to *show*).
- **`metricsDisplay()` output, read directly:** post-promotion, `facade`/`effLoss`/`glazedPct` all render `text: "[schematic-only — see Stage 2]"` with a `raw` field carrying the true number (542.6 etc. — never discarded, just not shown as the headline). `coverage`/`nVol` keep their numeric text and gain a `title` caption. `D1/D2/D4/D5/CAP6` were confirmed unflagged and — in this specific promotion — numerically **unchanged** (House's growth is entirely eastward/southward from an x/z corner that doesn't move, so the gaps `D2`/`CAP6` read didn't cross a threshold; `D1` *does* shift, 73.4→68.9 by hand-calc, because it's a real area-weighted average legitimately reacting to the box's new size — correctly unflagged, not a bug).
- **`containmentResults()` — an independent geometric proof, not asked for by the spec but the strongest evidence available:** 4 breaches before promotion (MB +2.9 m S, GB/MU +1.4 m E, ML +1.4 m E +3.0 m S — matching the code's own long-standing comment exactly) → **0 breaches after**. The promoted box now exactly contains every room that fed it, confirmed by a function that has nothing to do with `metrics()`/`metricsDisplay()` at all.
- **Trade-space bubble, pixel-checked (`getImageData`, not a screenshot guess), same method `Containment-And-TradeSpace-2026-09-09.md` used for the star-visibility fix:** sampled the star's exact pixel (via `chart.getDatasetMeta(0).data[0]`) before and after promotion. Before: `rgb(138,84,16)` = `#8a5410`, exactly the untainted `starBorder` color coded at `:527`. After: `rgb(107,100,89)` = `#6b6459`, exactly the tainted `starBorder` color. Real pixel evidence of the color change, not inference.
- **Export byte-identity re-checked:** `buildInterchange()` merged (captured **before** promotion, the fair comparison) vs. standalone, `generated` timestamp stripped — byte-identical, both passes. `.promoted` does **not** leak into `massing-interchange/v1`'s `volumes[]` (confirmed by string search on the serialized JSON) — `buildInterchange()`'s field list is explicit (`{id,type,x,z,w,d,h,base,glaz,...}`), never spreads the box object, so the new schema field is structurally incapable of leaking. `buildExportData()` (Test-Fit) — byte-identical merged vs. standalone.
- **Screenshots:** `_UI/_playwright/qa-scratch/stage3_after_promote_pass1.png` (side-by-side, full UI, promoted House selected — badge, tagged façade/glazed, greyed star all visible together), `stage3_trade_star_grey_pass1.png` (isolated chart canvas crop).

---

## Stage 4 — The badge, not a tab

**Built:** two surfaces, both reading `state[i].promoted`, no new page-level control added anywhere:
1. `fillVolPicker()` (`Massing-Studio.html:342-350`) appends `" — LOD 200"` to a promoted volume's `<option>` text, so every volume's stage is scannable in the dropdown without selecting each one.
2. `buildSliders()` (`:352-365`) writes a fuller badge into `#volBadge` (HTML at `:85`, directly under the volume picker) for whichever volume is currently selected: `"Stage 1 — LOD 100 (siting)"` or `"Stage 2 — LOD 200 (articulated) · promoted <date> from Test-Fit · was <old w>×<old d>m"`.

**Checkpoint:** promoted House in the merged page; confirmed its dropdown option changed to `"House (house) — LOD 200"` while Pool/Office/Link/Gym/Terrace's option text was byte-identical before and after (`['Pool (pool)', 'House (house) — LOD 200', 'Office (office)', 'Link (link)', 'Gym (gym)', 'Terrace (terr)']` — only index 1 changed). Confirmed the House badge itself flips from the Stage 1 line to the full Stage 2 line with the correct promotion date and pre-promotion dims. Confirmed zero new console/page errors. Exercised the existing scheme (A/B/C, via the real toolbar buttons) and view (Plan/Iso/Persp, real buttons) switchers, and re-confirmed the pane-layout switcher (already exercised in Stage 2's test): all four unaffected by the new markup — scheme A/C stayed correctly unpromoted (own `WORK` arrays), switching back to B correctly still showed House promoted (a promotion persists across scheme switches exactly like any other edit already does).

**A real bug, caught by this exact check and fixed before shipping.** The first version of `promoteVolume()` wrote `state[i]` correctly and called `rebuild()`/`refresh()`, so the 3D box grew immediately — but `refresh()` never calls `buildSliders()`/`fillVolPicker()` (an ordinary slider's own `oninput` patches only its own `<span>` directly, never the whole panel). Driving the real button via `page.click()` and reading the DOM immediately after — rather than my own test script's convenience re-invocation of `buildSliders()`, which had been silently masking this — showed the badge and the dropdown option both still reading Stage 1 / no `— LOD 200` suffix immediately after a real click, until the volume was re-picked by hand. **Fixed**: `promoteVolume()` (`Massing-Studio.html:851-870`) now also calls `fillVolPicker()` unconditionally (so the dropdown always reflects the promotion regardless of what's currently selected) and `buildSliders()` when the promoted volume is the one currently selected (so its own sliders/badge repaint immediately). Re-ran the full checkpoint sequence twice more after the fix — all prior results (metrics, containment, pixel colors, export byte-identity) held, and the badge/dropdown now update the instant the button is clicked, matching a real slider edit's behavior.

**Per §7's own deliberate-pause instruction:** Ben asked to build through Stage 5 anyway, so this did ship — but here, exactly, is where a "two tabs instead" reversal would land, kept localised on purpose:

| To change | File | Lines |
|---|---|---|
| Badge text/wording, both variants | `Massing-Studio.html` | `358-364` (`buildSliders()`'s `if(b.promoted){...}else{...}` block) |
| Badge DOM placement (currently: directly under the volume picker) | `Massing-Studio.html` | `85` (`<div id="volBadge">`) |
| Dropdown suffix (currently `" — LOD 200"`) | `Massing-Studio.html` | `347` |
| Whether a badge exists at all vs. a different control shape (e.g. Ben's original two-tab idea) | `Massing-Studio.html` | `342-365` is the whole surface — nothing about promotion state lives anywhere else in the DOM |
| The provenance flag itself (schema, not UI) | `Massing-Studio.html` | `856-857` (`promoteVolume()`'s `b.promoted = {...}`) |

Reversing the UI call never touches `promoteVolume()`'s write, `metricsDisplay()`'s gating, or `promotionPreview()` — those three are the mechanism; the badge is a thin, separable read of one flag.

---

## Stage 5 — Method propagation

**`_skill-source/references/phase-3-massing.md`** — new "## Stage 1 → Stage 2 — siting, then articulation" section (`:19-31`, right after "## Converge"), carrying: the two-sub-stage split with the LOD 100/200 correspondence; the metric-gating rule (gated/captioned/safe, matching §2's three buckets exactly); the badge-not-tab call with the "no demote button" reasoning. The `## Converge` section (`:14-16`) also gained the promotion decision-log row shape (`"Space planning → Massing"` phase-cell convention).

**`_skill-source/references/phase-4-space-planning.md`** — new "## The loop back to massing — a live containment breach is a signal, not just a note" section (`:24-28`), inserted right after the furnishing-order section and **before** "## The Test-Fit Studio," naming the cross-reference `Two-Stage-Integration-2026-09-09.md` §4 found missing: a containment breach is the trigger to consider a promotion, this is a phases-3/4 loop not a sub-phase sequence, and the drivers-lock-does-not-reopen caveat (with the `coverage`-is-the-metric-to-watch note). Confirmed line 32's (formerly line 26's) "Freeform by default — don't hard-require a massing export" still reads correctly immediately after — the new section frames the loop as conditional on a volume existing ("synced or live-linked from the Massing Studio"), consistent with, not contradicting, freeform-by-default.

**`_skill-source/SKILL.md`** — one new paragraph in the folded-in Phase 3 section (`:143`), the condensed Stage 1/Stage 2 one-liner the brief asked for. Phase 4's condensed section carries no folded content to update (re-confirmed by reading the full file — it's a references pointer only, line 75) — nothing added there, matching what `Two-Stage-Integration` already found.

**`GLOSSARY.md`** — three new entries after `Massing Studio` (`:80`): `Stage 1 (siting)` (`:81`), `Stage 2 (articulation)` (`:82`), `Promotion` (`:83`), each with the LOD cross-reference and the decision-log convention.

**A real contradiction with the spec, found and not papered over:** `Two-Stage-Integration-2026-09-09.md` §1 states *"`GLOSSARY.md` has no competing term, so this is new vocabulary, not a collision."* **This is wrong.** `grep` found `phase-2-site.md:17` and `:26` already define **"Stage 1 — zone scorecard"** and **"Stage 2 — parcel scorecard"** for the site-search process — a genuinely unrelated concept, already referenced in `GLOSSARY.md`'s own `Envelope-utilization` entry (`:59`, pre-existing, unedited). I did not rename massing's Stage 1/Stage 2 (per the same document's own reasoning: the names were already coined and used in two prior design documents before this collision was caught, and Ben hasn't weighed in on a rename), but I added an explicit disambiguation note to both new glossary entries — `(siting)`/`(articulation)` are now load-bearing disambiguators, not optional color, and each entry cross-references the collision directly so a future reader isn't left to discover it by grepping. **Flagging this for Ben:** if he'd rather rename one pair (e.g. massing's could become "Stage 1 (siting)/Stage 2 (articulation)" stays, site-search's could become "Zone stage/Parcel stage"), that's a clean, localized rename — four lines in `phase-2-site.md`, one in `GLOSSARY.md`'s `Envelope-utilization` entry — not touched here since it's outside this pass's scope and site-search's naming predates this work.

**Checkpoint:** `grep -n "Stage 1\|Stage 2" GLOSSARY.md` (re-run after the edits) confirms exactly the collision above and nothing else — no other term collides. Read both updated reference files end-to-end for internal consistency: the promotion mechanism is describable from the reference text alone (a "Reshape to fit" action, its trigger condition, its effect on `state[i]`, and which metrics it stales) without needing to read this report or the code. Did **not** spin up a full fresh-project seeding session (out of proportion to the rest of this pass's budget) — this is the one checkpoint item I'd call partially, not fully, run: I verified the text is self-consistent and complete by close reading, not by an actual from-scratch reproduction test.

**Skill repackaged** via `python -m zipfile` (CLI form, matching `HANDOFF.md`'s 2026-09-06 convention exactly) — **not** `python`'s `zipfile` module called directly, and never `Compress-Archive`. One real mistake caught before it shipped: a naive `_skill-source` → `design-process/` copy pulled in `_skill-source/design-collaborator-roster/SKILL.md`, which belongs to the **separate** `design-collaborator-roster.skill` package, not this one — the original archive never contained it (verified: 8 files before, 8 files after). Rebuilt with only `SKILL.md` + `references/*.md` copied. Verified byte-for-byte: every one of the 8 files inside the new `design-process.skill` matches its `_skill-source/` counterpart exactly (`SKILL.md` 34,618 bytes, all 7 references files, all `MATCH`). Archive uses forward-slash paths throughout (`design-process/references/phase-3-massing.md`, no backslashes) — confirmed by direct `zipfile.namelist()` read.

---

## Full-sequence stability

Ran the entire Stage 2/3/4 Playwright sequence **five independent times** across this session (not a one-off) — three before the `promoteVolume()` badge-refresh fix above, two after: every numeric result, every DOM text string, every pixel-color read, and every byte-identity check was identical across every run, both before and after the fix (the fix only changed *when* the badge/dropdown repaint, never any of the underlying numbers). Zero console warnings beyond benign headless-Chromium WebGL/Canvas2D performance notices (`GPU stall due to ReadPixels`, `willReadFrequently` advisory) — both are artifacts of running a 3D WebGL scene + repeated `getImageData` calls under software-rendered headless Chromium, not real app defects; **zero page errors** in every run, merged or standalone. Standalone `Massing-Studio.html` (fresh load, no promotion, no Test-Fit sibling): zero console/errors, `#promoPreview` correctly `display:none`, badge correctly reads `"Stage 1 — LOD 100 (siting)"` (the honest default). Standalone `Test-Fit-Studio.html`: zero console/errors — the one added line (`refreshPromotionPreview` guarded call) is a confirmed no-op there. Nyando re-confirmed unaffected: `Projects/Nyando-Maternity-Waiting-Home/3_Massing/` still contains no `Massing-Studio.html` (never created by this pass), its `Test-Fit-Studio.html` loads standalone with zero console/errors (screenshot: `nyando_unaffected_check.png`), and `phase-4-space-planning.md:26`'s freeform-without-massing sentence still reads correctly next to the new prose above it.

---

## What could not be verified / left honestly incomplete

- **Stage 5's "seed a fresh project" checkpoint** — verified by close reading, not by an actual from-scratch project build (see above).
- **The Stage-1/Stage-2 naming collision with site-search** — flagged, disambiguated, not resolved (Ben's call whether either pair gets renamed).
- **A "show promoted volumes" legend/highlight toggle** — `Two-Stage-Integration` §3 explicitly recommends *against* building this speculatively, deferring it behind real use of the badges. Not built, per that recommendation.
- **`massing-interchange/v1`'s `promoted`/`stage` field** — deliberately not added, per §8's own recommendation to defer until a real downstream (Rhino/Revit) consumer asks for it. Confirmed (not just assumed) that `.promoted` cannot currently leak into that schema by construction.

---

## What Ben should click, and what he'll see

**Layout note first, since it changes which of these is visible:** at 1440×900 side-by-side, Test-Fit's own pane has only ~41px of clear plan width (`Review-Steps-1-4-2026-09-09.md`'s own measurement, re-confirmed this session) — and this session found the *Massing* pane's promotion-preview panel is similarly cramped in the narrow ~270px `#left` column. **Use "Massing only" or "Plan only" from the shellbar's Layout buttons** for anything below, not side-by-side, unless just spot-checking that both panes still render.

1. **Open `Projects/Nonimuss-Residence/Design-Studio.html`.** Click **Massing only**. House is selected by default (it's scheme B's `selIdx=1`).
2. **Look at the left panel, right under the volume dropdown.** You'll immediately see: a small grey line reading `Stage 1 — LOD 100 (siting)`, and — a few lines down, under the Rotate button — an amber-colored box reading `Promotion preview (live from Test-Fit — no write yet): House: 11.6×9.3 → 13.0×12.3 m ... Reshape to fit`. In the 3D view, a dashed amber wireframe box is visible around House, slightly larger than its current solid grey box — that's the same preview, drawn in 3D. **This is already live with zero clicks** — House's rooms have always overrun its box (conflict #1), so the preview has something real to show from the moment the page loads.
3. **Click "Reshape to fit."** House's box visibly grows and its sliders jump to `Width 13.0` / `Depth 12.3`. The badge changes to `Stage 2 — LOD 200 (articulated) · promoted <today's date> from Test-Fit · was 11.6×9.3m`. In "Cap & metrics" below, **Façade (envelope)** and **Glazed façade** now read `[schematic-only — see Stage 2]` in amber instead of a number — hover either for a tooltip explaining why. **Lot coverage** and **Build simplicity** still show real numbers but now carry an explanatory tooltip on hover (the visible text is unchanged — hover to see the caption). Scroll down to **Driver proxies**: **D3 winter**'s bar is now pale grey instead of colored, with `[schematic-only — see Stage 2]` next to its number.
4. **Scroll to Trade-space.** The orange ★ has turned grey — it sits very close to (in this case, exactly on top of) scheme B's own grey reference bubble, so look for the readout line just below the chart: `live now: cap-safety 80 · D2 94 · D3 (bubble size) 51 [schematic-only — see Stage 2]`. The caption line at the very bottom (`A/B/C reference bubbles are always Stage 1...`) explains why only the ★ can grey out.
5. **Click "Plan only."** Switch to the Test-Fit pane. The dashed green "House — live massing cut" outline is now visibly bigger than before, and MB/GB/MU/ML — previously drawn with a red dashed outline and a `⚠` "outside House's footprint by..." label — are now drawn as ordinary rooms with no warning at all. That's `containmentResults()` reporting zero breaches, a second, independent confirmation the promoted box is correct.
6. **To see it un-promoted again:** click **Reset scheme** in the Massing pane (top-left toolbar) — this discards all edits including the promotion and returns to scheme B's authored defaults, the same reset button that's always existed.
