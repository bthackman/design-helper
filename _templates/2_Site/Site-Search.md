# Site search — [Project name]

*Used when the site is not yet chosen. Two stages; each ends with a user decision. If the site is already known, skip to Site-Details.*

## Selection criteria (from client motives + brief)
Not every motive earns a column below — keep only the ones actually discriminating between candidates (mirrors the precedent phase's driver-weighting elicitation).

| Motive | Weight (H/M/L) | Source (client discovery / brief) |
|---|---|---|
|  |  |  |

## Stage 1 — zone scan
1. Selection criteria from client motives + brief (water, character, commute ceilings, sun, access...)
2. Candidate zones (3–5), scored below
3. Named trade-off: what the criteria make impossible, and the defensible reading
4. Verdict per zone: drill down / cut (cuts documented — they strengthen the final argument)

Glyphs: ✓ meets · ~ partial · ✗ fails this motive. Flag a cell `[proxy, unverified]` where the score is impression, not a verified fact. Add/remove motive columns to match the criteria table above.

| Zone | Access (state units!) | Character | Jurisdiction | M1 | M2 | M3 | Caveats | Verdict |
|---|---|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |  |  |

Named trade-off:

## Stage 2 — parcel drill-down
1. Verify zone land use via municipal open data (district codes, adjacency to park/water reserves)
2. Satellite review → candidate parcels/bands (link exact map-view URLs, don't embed screenshots)
3. Scorecard vs. client motives, below — plus **envelope-utilization** (target GSF ÷ buildable envelope GFA: the same setback/height/FAR math `Site-Details.md` runs for real once a site is chosen, run here per-candidate). A parcel that scores well on every motive but can't fit the program should lose visibly here, not surface as a later surprise.
4. User selects → log decision with rejected alternatives → create `Site-Details_[address].md`

Same glyphs as Stage 1.

| Parcel | Land-use verified? | M1 | M2 | M3 | Envelope-utilization | Caveats | Verdict |
|---|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |  |

Recommendation + runner-up:
